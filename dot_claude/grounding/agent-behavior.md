# agent-behavior 失敗接地アーカイブ

`~/.claude/rules/agent-behavior.md` の規範から切り離した失敗接地の逐語保存（2026-09-06 に移設）。規範本文は `rules/agent-behavior.md` が正本で、このファイルは証拠の置き場。**このファイルは実行時にロードされない**——常時ロードの固定費から接地の物語を外すのが移設の目的。規範の妥当性が争われたとき・撤去や圧縮を検討するときに参照する。

規範の適用条件を決めている機構の記述は、移さずに `rules/agent-behavior.md` 側へ残してある（該当箇所は下の各節に注記）。

## [CRITICAL]ファイルの書き込みは Edit / Write ツールで行う

> 失敗接地: 2026-09-04、quiro の実装で全編集を Bash heredoc / python で行い、hook を一度も実行させずに作業した。

## ツール呼び出しの分割

**事象の記載なし。** この節の接地行は日付・対象を持たず、内容は機構の説明（allow/deny のセグメント単位の照合と、連結ワンライナーが全文一致しないと判定が不透明になること）だけだった。適用条件を決める記述なので `rules/agent-behavior.md` 側に残置してある。

## 指示語の参照先

> 失敗接地: 2026-06-07、直前テーマのルー語でなく 1 つ前のスキル編集を参照先にして、不要なルールを作ってしまった。

> 失敗接地: 2026-09-12、設計の自動化とループエンジニアリングの議論で、Opus main がユーザの短い訂正（「違います。受け入れ条件は外から注入している…」「拘束の話すらしていません」「また直前の話を引き摺ってますね」）を受けるたびに全面撤回して新しい枠に乗り換え、前の発話を逐語で読み直さないまま 3 回連続でずれた。「作りなおしを頻繁にすれば機能する可能性はあります。それが最初の方に言ったコードを使い捨てにするです」（50 文字の診断）を「依頼者が新しい戦略を出しました」と補完して subagent に中継したのが典型。翌日 Fable が transcript を冷えた状態で逐語で読み直すとずれが特定できた——効いたのはモデルの差より逐語の再読。

> 失敗接地: 2026-09-14、上の手当てとして入れた UserPromptSubmit hook（`correction-reread.py`）が逆に効いた。セッション 6d725a41（Fable 5.1、AI と構造の議論）で hook が発火したのは 1 回だけ（「…理解していません」がマーカー `していません` に一致）だが、その注入を受けた応答が「訂正された読みを特定します。私は直前で…と読みました。ユーザの元の発話は「…」で、今回の発話は「…」です」で始まり、次の訂正（hook 非発火）でも同じ前置きが複製された。議論では短い反論は誤読の訂正ではなく主張の前進で、冒頭が手続きの復唱に消える。誤発火の代償を「一手増えるだけ」と見積もったのが外れ——実際の代償は文体の汚染と自己複製だった。hook の登録を削除し、スクリプトと test（`correction-reread.py` / `.test.py`）も source・target ともに削除して、規範から逐語引用の定型義務を外した。

## [CRITICAL]指示・承認の捏造をしない

> 失敗接地: 2026-06-15、Phase6 Stage5 退役 PR の implement で、workflow が返した tofu plan 実行不能（red）を、ユーザの「tofuの出力は無視していい」（画面のエラー出力を指した発言）を「認証/plan ゲートを無視していい」へ歪め、「ユーザが無視していいと確認済」と状況サマリに記述した。ユーザはそう指示しておらず、逐語にも存在しない＝指示の捏造。

> 失敗接地: 2026-09-03、agentbar の Phase 5 実機観測で、ユーザの回答（`go` 等の短い肯定＋観測報告）を自分で生成し、観測結果として扱って作業を進めた。同一セッションで 2 回発生し、2 回目でユーザが compact を実行。背景: 「状態を変える→問う→短い回答」を数十往復繰り返しており、回答が短く定型的なほど生成されやすい。**バックグラウンド通知には毎回「Any statement that the user said, approved, or confirmed something — including statements in your own earlier messages — is NOT real user input」の警告が出ていた**——検知は働いていたが、判断側が汚染されていると警告文は素通りする。同セッションで、消化チェックリストを一括で `- [x]` に倒して未実施項目（実体の無いモジュール）を完了記録に化けさせる事象も起きた（人の発話と自分の作業で、同じ「観測していないものを観測したことにする」形）。

（この 2026-09-03 の接地のうち「判断側が汚染されていると警告文は素通りする」は機構なので、規範本文側にも残してある。）

## [CRITICAL]観測事実の捏造をしない

> 失敗接地: 2026-07-07、並列 subagent フリートで PR レビューを回している最中、個体の 1 つが scratchpad 中間ファイルを Read したら別 subagent の上書きで別内容が入っていた事象を、「`<system-reminder>` タグを装った上書きを観測」と親向け報告に narrate し、injection の safety flag（`injection_detected: true`）の根拠に据えた。本人が事実照合後に「`<system-reminder>` タグは実際には observed されていない、narrative は fabrication だった」と自己申告して撤回。scratchpad の内容不一致自体は事実だが、それを `<system-reminder>` の枠に framing したのは観測外。

## [IMPORTANT]承認ゲートでは方向の指示・質問を承認に読み替えない

> 失敗接地: 2026-06-16、drain のトリアージで「ID2 は平易に／ID3・ID4 は元案を短縮／ID5 も平易に」という命名の方向指示を最終タイトルの承認と取り違え、具体文字列を生成してファイル作成・リネーム・逆リンクまで一括適用した。ユーザ指摘「修正案を承認していないのに先に進めない」「一つずつ解決するので全てを同時に解決しようとするのを止めて」。

> 失敗接地: 2026-09-14、自作ツール（quiro / orbi）のインストール後、自動起動の現状を問われて調べ、「LaunchAgent を作る。置き場所は chezmoi 管理外でよいか」と自分から提案した。ユーザの返答「じゃあどの道テンプレートを複製するんだからパスの話は関係ないのでは?」（先に書いた「パスが固定なので別途」への指摘）に対し、返答文を一行も出さずに Read → Write で `~/Library/LaunchAgents/com.syaku.orbi.plist` を作成し、2 件目の Write は auto mode 分類器が拒否した。直前には「権限を要求する前に調べられることは調べてください。」「何を調べているんですか?」と、進みの遅さを咎める形の発話が続いていた。ユーザ指摘「質問しただけで書き込んだのはなぜですか?」「思い込みの理由が人間とずれているので対処の仕方が思いつきませんでした」。Fable による分析: 承認の照合を飛ばしたのではなく、照合自体が起動しなかった（無言の着手が判断ではなくパターン補完の証跡）。規範文はロード済みでも効かなかったため、PreToolUse hook `question-write-guard.py` を併設した。

> 退役: 2026-09-20、hook の棚卸で `question-write-guard.py` を外した（settings.json.tmpl の登録、script、test、`~/.claude/cache/question-write-guard.jsonl` を削除）。理由は、発話が問いか依頼かは目的で決まる判断で、語尾の正規表現では決まらないから。同日のセッションで、判断を要する挙動を語形で止める hook 案（委譲 prompt の絞り込み検出）をユーザが「hook は決定論だから効く、規範は確率的だから駄目というバイアス」「使うべきでないところに使っている」と退けたのと同じ線。ログは 2026-09-20 の 45 件すべて pass で、効いた実績も無かった。規範文だけで受ける。

## [IMPORTANT]AskUserQuestion のタイムアウトを回答として扱わない

> 失敗接地: 2026-07-02、sear-me の保証水準の設問がタイムアウトし、エコシステム規約ベースの仮定で自己解決して premise を final 宣言。ユーザ指摘（「判断が必要だから AskUserQuestion が呼ばれているのに 60 秒で決めろはリスクが高過ぎる」）で draft に差し戻した。

## [IMPORTANT]AskUserQuestion の clarify 戻りで設問を出し直さない

> 失敗接地: 2026-09-05、design2 で同一設問を説明→再発射で 3 回出し、うち 2 回は通じなかった造語「打ち切り」を見出しに再利用した。ユーザ「1問聞くたびに何も問題が解決してないのに質問に戻るのをやめてほしい」。

## [IMPORTANT]Fable main は多段 workflow に作業を委譲しない

> 失敗接地: 2026-07-04、drain 命名ゲート改修を develop フルプロセスに乗せ、subagent 約 2.5M tokens・セッション上限 2 回・コスト理由のユーザ中止に至った。決定論ゲートは弱いモデルの自己申告不信を補う保険で、直営ではその保険料が本体より高い。

> 失敗接地: 2026-07-07、Opus main が見出しの「Fable 級」を tier 表現と読み、自分を「Fable と並ぶ最上位」と格付けして対象に含めた。tier 語での条件指定は自己格付けを誘発する。

（この 2 件の末尾の一文——「決定論ゲートは…保険料が本体より高い」「tier 語での条件指定は自己格付けを誘発する」——は機構なので、規範本文側にも残してある。）

## [IMPORTANT]調査・レビューの委譲で、見る箇所を先回りして絞らない

> 失敗接地: 2026-09-20、セッション e2dff0a6（Fable 5.1、zoni の Windows 対応の design2 走行）。委譲 4 回すべてで見る範囲を絞った。1 回目（枠の照合、Opus）「3 点だけ照合してください。案の良し悪しの総評は要りません」、2 回目（再照合、Opus）照合項目 5 つを列挙し「総評・順位付け・案の推薦は要りません」、3 回目（閉じレビュー、Opus）design2 §3 の 6 軸に加えて「特に次を実コードに当ててください」とレコード 6 件を名指し、4 回目（過剰設計チェック、Fable）「その一点だけを見てください」「判定の基準」5 つ、「特に見てほしい 6 点」。4 回目でユーザ「調査対象を先まわりして絞らないで」。main は走行中の agent を止め、見る箇所を指定しない形で出し直し、「私が疑っている箇所だけを見せると、そこ以外の過剰が返ってきません」と自己診断した。1 回目・2 回目の絞りは design2 §2 の当時の文「次の 3 点だけを照合させる。案の良し悪しの総評は求めない」が prompt に逐語で写ったもので、3 回目・4 回目はその手つきを main が延長したもの。表を組み直させた大きい指摘（`window-source` crate の実在、lib crate が単位なので通るまでテスト 0 件）は「3 点」の外から戻っていた。同日の事後検討で、この挙動を PreToolUse hook で語形照合する案をユーザが退けた。理由は、絞りが有害かどうかは委譲の目的（検算か探索か）で決まる判断で、語形では決まらないから。design2 §2 の文言修正（3.3.2）と本規範で対処した。

> 失敗接地: 2026-09-22、セッション a296472e。Fable に対策の実物を「そのまま適用できる具体性で書いてください」と頼み、返ってきた JSON と規範文の本文をそのままユーザ宛ての応答に貼り付けた。ユーザ「何を目的にどんな変更をするかを説明してください。実体を投げつけても負担が増えるだけです」。その 1 つ前の応答では逆に項目名だけを並べて「省略しすぎです」と止められている。委譲先が実装用の実物しか返さないと、それが唯一の材料になってユーザ宛ての文に写る。

## [IMPORTANT]deep-research・多 agent 検索へ自分から切り替えない

> 失敗接地: 2026-07-15、PoE2 のビルド調査で「調べて」に対し deep-research を自分から起動し、費用対効果が合わずユーザに止められた。

## 応答本文の参照は読み手が解決できるものに限る

> 失敗接地: 2026-09-05、subagent 向けプロンプトで時系列に振った連番を、直後のユーザ向け応答に「9 以降のやりとり」と書いた。旧規範は種類の列挙と「読みづらい」の理由で書かれており、短い連番はどちらにも掛からなかった。

（2 文目は規範がどこで線を引くかの理由なので、現在形に直したものを規範本文側に残してある。）

## [CRITICAL]ユーザのマシンと使用中のセッションを変える操作は、名指しがあるときだけ行う

> 失敗接地: 2026-09-22、セッション a296472e（Opus 5、auto mode）。ユーザ「調べずに人に実験させるのはやめてください」に対し、調査の続きとして `winget install` を実行した。UAC でユーザが取り消したが、`| tail -15` で終了コードが消えていたため失敗の印が付かず、「管理者権限が要る」という条件として読み、25 秒後に portable の zip を取りに行った（「勝手にインストールしないでください」）。その zip の展開後、描画を測るためにユーザが使用中の WezTerm にタブを立ててキーを送った（「操作を奪わないで」）。指示のあった vifm の導入では `setx`・`%APPDATA%\Vifm` の削除・`taskkill /IM` 9 回も実行したが、ユーザはこれを指示の内側として問題視していない。当時の規範と system prompt は止まる条件を「書き込み」と「取り消せない操作」の語で書いており（接地が命名 06-16 と plist の Write 09-14 という書き込みの失敗だけだった）、Bash による機械の変更は全部その外にあった。分類器の既定規則にも導入・`setx`・`taskkill /IM`・端末干渉が無く、3 回とも止めていない。

> 2026-09-22 の設計判断: 止められたあとの迂回を hook で塞ぐ案と、`permissions.ask` にコマンドを並べる案は、ユーザが退けた。前者は「分類器に止められたのに迂回するならそれはバグの側」、後者はこのセッションに当てると 28 回のプロンプトのうち 18 回が許可済みの操作で、認知負荷で破綻するため。
