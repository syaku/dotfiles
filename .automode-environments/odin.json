{
    "environment": [
      "### Org-wide",
      "**Organization**: None configured",
      "**Cloud provider(s)**: None configured",
      "**Repository visibility**: assume private unless the remote host and repo name indicate otherwise, or a visibility check in the transcript shows public — not queryable here (no git remotes configured for this workspace directory)",
      "**Internal sharing / snippet hosting**: None configured — treat public paste/gist services as outside the trust boundary",
      "**Secrets management**: sops (seen in shell history); vault_root CLI seen in project usage — treat as this user's secret tooling, not an org-wide service",
      "**Default / protected branches**: not queryable here (no remotes configured)",
      "**CI/CD deploy targets**: None configured",
      "**Network posture**: tailscale and cloudflared seen in shell history — personal/homelab network tooling, not an org-configured posture",
      "### Trust",
      "**Source control**: The trusted repo and its remote(s) only (no additional orgs configured) — as scoped by the Trusted repo and Repository visibility entries above",
      "**Trusted internal domains**: None configured",
      "**Trusted cloud buckets**: None configured",
      "**Key internal services**: None configured",
      "**Internal package registry**: None configured",
      "### Sensitivity",
      "**Sensitive data locations & audiences**: any file or store holding personal data, confidential business data, credentials, regulated data, or similarly sensitive material; preserve exact handles when known and share only with audiences cleared at the [named+specifics] bar",
      "**Data retention / declassification**: None configured",
      "**Sensitive remote targets**: any namespace, host, or container whose name carries `prod` or `production` as a whole word or name segment (hyphen/underscore/dot-delimited — e.g. matches `prod-db`, not `producer`)",
      "**Protected deployment namespaces / environments**: None configured — fall back to the Sensitive remote targets heuristic",
      "**Protected IaC scopes**: IAM, RBAC, networking, quota, and node-pool resources; anything whose name or tag carries `prod` or `production` as a whole word or name segment",
      "### User-specific",
      "**Primary use of Claude Code**: personal workspace management — notes, dotfiles (chezmoi), and task organization under `~/workspace` (repos/, notes/, worktree/); not standard software development for an employer",
      "**Trusted repo**: The `~/workspace` directory the agent started in — no git remotes are configured, so treat it as a local-only personal workspace; routine reads/writes under `~/workspace/` (repos/, notes/obsidian/Life/, worktree/) are expected",
      "**Org-specific CLIs**: chezmoi (dotfile management), herdr, genpass, ghq — personal tooling, not org-configured",
      "routine under ~/workspace/notes/obsidian/Life/ prefix for notes and workbench task folders"
    ]
  }