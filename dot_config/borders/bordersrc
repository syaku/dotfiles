# JankyBorders 設定。AeroSpace の after-startup-command から exec される。
# 直接 ~/.config/borders/bordersrc から実行されるわけではない (AeroSpace 側で引数指定)。
# ここでは独立起動向けのパラメータを記す。

options=(
    active_color=0xffe1e3e4
    inactive_color=0xff494d64
    width=5.0
    style=round
)

borders "${options[@]}"
