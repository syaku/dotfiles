.config/nushell/history.txt
.config/nvim/lazy-lock.json
.glaze-wm/errors.log
.glzr/glazewm/errors.log
.config/doublecmd/history.xml
.config/doublecmd/tabs.xml
.config/doublecmd/session.ini
.config/doublecmd/doublecmd.err
.config/doublecmd/doublecmd.inf
.config/doublecmd/ignorelist.txt

# zsh キャッシュと履歴
.config/zsh/.zcompdump*
.config/zsh/.zsh_history
.config/zsh/.zsh_sessions/
.config/zsh/zcompcache/
.config/zsh/.zcompcache/
.config/zsh/local-history
.config/zsh/history.local
.config/zsh/.histfile

# プラグインのキャッシュやgit情報
.config/zsh/plugins/**/.git/
.config/zsh/plugins/**/.gitignore
.config/zsh/plugins/**/cache/

# Claude Code — 動的ファイル（管理不要）
.claude.json
.claude/projects/
.claude/statsig/
.claude/cache/
.claude/history.jsonl
.claude/shell-snapshots/
.claude/session-env/

# rules 配下の local-* はこの環境固有の規範。chezmoi に取り込まずローカルに閉じる
.claude/rules/local-*.md

# リポジトリ用ドキュメント（target に展開しない。先頭 / でルート限定）
/CLAUDE.md

# バックアップと一時ファイル
.config/zsh/*~
.config/zsh/*.zwc
.config/zsh/.*.zwc
.config/zsh/*.old
.config/zsh/*.bak
.config/zsh/*.tmp

# Brewfile はプライベートな mac でのみ展開。業務端末は ~/.config/brewfile/Brewfile をローカル管理する
# （isPrivate 未定義の環境では安全側に倒して ignore。mac 以外は brew 非対象なので ignore）
{{ if or (not (dig "isPrivate" false .)) (ne .chezmoi.os "darwin") }}
.config/brewfile/Brewfile
{{ end }}

# AeroSpace 系一式 (タイル WM、JankyBorders、karabiner) は mac でのみ展開
{{ if ne .chezmoi.os "darwin" }}
.config/aerospace
.config/borders
.config/karabiner
{{ end }}

# karabiner-elements は backup を automatic_backups/ に大量に吐く。展開対象外
.config/karabiner/automatic_backups

# GlazeWM (タイル WM) / zebar (ステータスバー) / tacky-borders (フォーカス枠線オーバーレイ)
# は Windows 専用ツールなので Windows でのみ展開
{{ if ne .chezmoi.os "windows" }}
.glzr
.config/tacky-borders
{{ end }}

# 上記ツールの動的ファイル・公式サンプル widget は Windows 側でも管理対象外
.glzr/zebar/errors.log
.glzr/zebar/tmp-*
.glzr/zebar/boilerplate-*
.glzr/zebar/starter
.config/tacky-borders/tacky-borders.log

# SSH — 頻繁に変化する known_hosts と除外対象の古い .pem は管理しない
.ssh/known_hosts
.ssh/known_hosts.old
.ssh/*.pem
.ssh/*.pem.pub

# 個人 SSH 鍵 (id_ed25519) と接続情報 (config, personal infra のホストを含む) は
# private 環境のみ展開する。isPrivate 未定義の機は false 扱いで安全側に倒し ignore（鍵を置かない）
{{ if not (dig "isPrivate" false .) }}
.ssh/config
.ssh/id_ed25519
.ssh/id_ed25519.pub
{{ end }}

# conf.d のローカル専用 drop-in（業務端末の Volta/SDKMAN 初期化等）は同期側に出さずローカルに閉じる
**/conf.d/*.local.zsh
